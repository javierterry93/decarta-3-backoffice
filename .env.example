# local = Zustand + localStorage (por defecto)
# remote = HTTP contra openapi.yml
VITE_MENU_API=local

# Base URL del backend (modo remote)
VITE_API_BASE_URL=/api

# Proxy de Vite en desarrollo (solo npm run dev)
VITE_API_PROXY_TARGET=http://localhost:3000
